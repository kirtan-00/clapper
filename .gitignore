node_modules
dist
dist-single
dev-dist
*.local
*.tsbuildinfo
.DS_Store
.vercel

credentials.md
supabase/.temp/
dist-site

# secrets - never committed
.env
.env.*
!.env.example
.superpowers/

# Business planning that names the owner, his agency and his pricing strategy.
# Stays on disk, never in a public repo.
docs/validation-and-willingness-to-pay-plan.md

# Desktop cleanup landing zone: deliverables, screenshots, reports, and scripts
# filed off the owner's Desktop. Can run into the hundreds of MB, so it stays
# local and untracked rather than bloating the repo.
_archive/

# Agent worktrees and session scratch. Worktrees are checkouts of this same
# repo; committing the directory would nest a repo inside itself.
.claude/

# Screenshot harness output (scripts/shoot-*.mjs). Regenerated on demand and
# measured in megabytes; the harness is tracked, the pictures are not.
.shots/
.shots-onboarding/
.shots-newproject/
.shots-studio/

# CDP harness scratch profiles
.chrome-recovery-profile/

# Outreach + email tooling: these carry real user and prospect email addresses.
# This repo is public. Keep them local; they are untracked on purpose, and
# untracked files survive branch switches so nothing is at risk of loss.
/outreach/
/scripts/send-cold.mjs
/scripts/send-thankyou.mjs
/scripts/send-agency-in.mjs
/scripts/send-agency-us.mjs
/scripts/seed-email-sends.mjs
/scripts/grant-thankyou-credits.mjs
/scripts/email-suppressions.txt
/docs/gtm-council-2026-09.md
